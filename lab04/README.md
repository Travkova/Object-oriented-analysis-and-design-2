## Предметная область
Система вопрос-ответ по вселенной "Трансформеры: Прайм". Приложение предназначено для поиска характеристик персонажей (имя, фракция, занимаемая должность, альт-форма, оружие) 
на основе структурированных данных, хранящихся в реляционной базе данных.

## Проблема
При прямой реализации взаимодействия между пользовательским интерфейсом и источником данных возникают следующие проблемы:
1. Громоздкий код: для работы с БД нужно писать sql-запросы, обрабатывать результаты и управлять соединениями. Это добавляет много лишнего кода.
2. Нарушение принципа разделения ответственности: код, который отвечает за отображение информации пользователю, будет зависеть от того, как устроена база данных (какие таблицы, какие колонки).
3. Риски при внесении изменений в систему: если потребуется поменять БД или подключить внешний API потребуется масштабный рефакторинг всех модулей, обращающихся к данным.
## Решение
Для решения данной проблемы применён архитектурный паттерн проектирования "Шлюз". В системе реализован класс CharacterGateway, который выступает единой точкой входа и полностью инкапсулирует доступ к источнику данных. 
## Диаграмма классов
<img width="1115" height="241" alt="диаграмма_классов_4лр" src="https://github.com/user-attachments/assets/0c5bc980-7a01-40e2-b9a2-14f9237325ac" />

## Вывод
При реализации системы без паттерна графический интерфейс напрямую управлял JDBC-соединениями, содержал SQL-запросы и логику парсинга ResultSet, что привело к нарушению принципа единственной ответственности и высокой связности между слоем представления и источником данных. Архитектурный паттерн **Шлюз** устранил эти недостатки: клиентский модуль GatewayApp теперь взаимодействует с системой исключительно через удобный интерфейс шлюза, не содержа ни одной строки кода, связанного с БД.
## Инструкция к запуску
1. Установить JDBC-драйвер для SQLite и положить в папку с программным кодом: https://github.com/xerial/sqlite-jdbc/releases
2. В окне терминала запустить команды:
   - javac -cp ".;sqlite-jdbc-3.53.1.0.jar" *.java
   - java -cp ".;sqlite-jdbc-3.53.1.0.jar" Main
